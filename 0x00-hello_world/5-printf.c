#include <stdio.h>

/**
 * main - this is the main function for printif
 *
 * Return 0 if successful
 */  

int main(void)
{	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
